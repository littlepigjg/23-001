# 缺陷复现报告（Bug Reproduction Report）

## 1. 问题概述（Summary）

固件升级系统在处理大批量设备操作时出现崩溃。当升级任务涉及的设备数量较多（超过约10个），或一次批量上报的升级进度数量较多时，系统会触发 `index out of range` panic，导致任务无法正常启动或进度无法正常上报。小批量操作（3-5个设备）不受影响。

## 2. 环境信息（Environment）

- 操作系统：Linux
- Go 版本：go 1.22.0
- 项目模块：fwupgrade
- 运行参数：无特殊参数要求
- 硬件信息：与硬件无关，纯软件逻辑缺陷

## 3. 复现步骤（Steps to Reproduce）

1. 进入项目根目录，执行 `go build ./...` 确保编译通过
2. 准备测试数据：创建1个设备型号、1个固件版本、12个该型号下的设备
3. 创建一个"指定设备升级"类型的任务，将所有12个设备加入目标列表
4. 调用 StartTask 启动任务
5. 观察系统输出

## 4. 实际结果（Actual Behavior / Observed Output）

- 调用 StartTask 时触发 panic：`index out of range [6] with length 6` 或类似越界错误
- 调用 BatchReportProgress（传入10条以上报告）时触发 panic
- RED/GREEN 判定结果：RED（缺陷未修复）
- 任务未能正常启动，升级记录未创建
- 批量进度上报中断，部分设备的进度状态丢失

## 5. 期望结果（Expected Behavior）

- StartTask 正常执行，12个设备的升级记录全部创建，任务状态变为运行中
- BatchReportProgress 正常处理所有报告，进度更新无误
- 无 panic 发生
- RED/GREEN 判定结果：GREEN
- go build 与 go vet 全部通过

## 6. 触发频率（Frequency）

- 必现（100%）：当目标设备数量或进度报告数量超过预分配容量（约为实际需求的50%）时必定触发
- 小批量操作（≤5个设备）不会触发
- 仅在涉及切片预分配的批量操作中出现

## 7. 影响范围（Impact / Scope）

- 服务崩溃：涉及多设备的升级任务无法启动
- 数据不一致：批量进度上报中断导致设备升级状态与实际不符
- 功能受限：系统只能处理小批量设备升级，无法支撑生产环境的大规模固件部署
- 可靠性下降：任何涉及较多设备的操作都存在崩溃风险

## 8. 附加说明（Additional Notes / Workaround）

临时规避方法：将大批量设备分成多个小批次（每批不超过5个）分别操作，可暂时绕过该问题。但这只是临时方案，不适合作为长期解决方案。