# 缺陷复现报告（Bug Reproduction Report）

## 1. 问题概述（Summary）
在固件升级系统的灰度发布功能中，当灰度比例设置为10%、20%、30%等整十数值时，灰度任务启动后无法正确分配设备，表现为0台设备被选中。但5%、15%等非整十比例值工作正常。该问题影响灰度发布流程的正常使用，导致无法使用常用的10%灰度比例进行小范围测试。

## 2. 环境信息（Environment）
- 操作系统：Linux
- Go 版本：go version go1.21.x
- 项目模块：fwupgrade
- 运行参数：go test . -v -run '^TestRedGreen$'
- 硬件信息：与并发/性能无关

## 3. 复现步骤（Steps to Reproduce）
1. 进入项目根目录，执行 go build ./... 确保编译通过
2. 准备测试数据：创建设备型号，创建至少100台设备关联到该型号
3. 创建固件版本，关联到同一型号
4. 创建灰度升级任务，设置灰度比例为10%
5. 启动该任务
6. 观察任务的TotalDevices字段，以及GenerateDeviceGroup的返回结果

## 4. 实际结果（Actual Behavior / Observed Output）
- 具体错误信息：无显式错误信息，但TotalDevices为0
- GenerateDeviceGroup(devices, 10.0)返回的灰度组大小为0，等待组大小为100
- StartTask创建的10%灰度任务TotalDevices为0
- RED/GREEN 判定结果：RED（红灯，缺陷未修复）
- 其他异常现象：任务状态显示运行中，但实际没有设备参与升级
- 非整十比例值（如5%、15%）工作正常，能正确分配设备

## 5. 期望结果（Expected Behavior）
- 10%灰度比例时，100台设备中应有约10台被选中（5-15台范围内均可接受）
- GenerateDeviceGroup(devices, 10.0)应返回约10台设备在灰度组中
- StartTask创建的10%灰度任务TotalDevices应为10左右（5-20范围内）
- 20%、30%等整十比例值也应正常工作，对应比例的设备数量被选中
- go test 验证命令单次运行判定为 GREEN（绿灯，缺陷已修复）
- go build 与 go vet 全部通过

## 6. 触发频率（Frequency）
必现（100%）：当灰度比例为整十数值（10%、20%、30%、40%、50%、60%、70%、80%、90%）时，100%出现0设备问题。

## 7. 影响范围（Impact / Scope）
- 灰度发布功能完全无法使用常用的整十比例值
- 用户无法使用10%、20%等直观的比例进行灰度测试
- 可能导致发布流程延误，需要使用非整十比例绕过问题
- 影响灰度策略的灵活性和易用性

## 8. 附加说明（Additional Notes / Workaround）
临时规避方法：使用非整十的灰度比例值，如5%、15%、25%等，可以正常工作。但这只是权宜之计，应尽快修复根本问题。
