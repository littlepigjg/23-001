# 缺陷复现报告（Bug Reproduction Report）

## 1. 问题概述（Summary）
固件升级管理系统在上传固件文件时存在两个异常行为：一是当 MD5 校验失败时，已上传到服务器的固件文件未被清理，持续占用磁盘空间；二是当上传版本号冲突（重复版本）时，系统内部的 ID 计数器被消耗，导致后续成功上传的固件 ID 编号不连续，出现跳号现象。

## 2. 环境信息（Environment）
- 操作系统：Linux (x86_64)
- Go 版本：go1.21 及以上
- 项目模块：fwupgrade
- 依赖管理：Go modules
- 运行参数：无需特殊运行参数，单即可复现

## 3. 复现步骤（Steps to Reproduce）
1. 进入项目根目录，执行 `go build ./...` 确保编译通过。
2. 创建临时目录用于存储上传文件，或使用项目默认的 uploads 目录。
3. 初始化一个固件型号（DeviceModel），获取其 modelID。
4. **文件残留复现**：上传固件时传入一个与实际文件内容不匹配的 MD5 值，观察上传目录中是否残留文件。
5. **ID 跳号复现**：先上传固件版本 v2.0.0（成功），再上传相同版本 v2.0.0（应失败），然后上传新版本 v2.0.1（成功），检查三个固件的 ID 是否连续递增。

## 4. 实际结果（Actual Behavior / Observed Output）
- **文件残留**：上传接口返回 MD5 mismatch 错误后，上传目录下的 model_X 文件夹中仍然存在已写入的固件文件（如 model_X_v_Y.Z.W.bin），未被自动清理。
- **ID 跳号**：假设首次上传 v2.0.0 成功得到 ID=2，重复上传 v2.0.0 失败后，再上传 v2.0.1 成功得到的 ID=4，跳过了 ID=3。ID 序列不再连续。
- 测试判定：RED（红灯，缺陷存在），测试命令退出码为 1。

## 5. 期望结果（Expected Behavior）
- **文件清理**：上传接口返回 MD5 mismatch 或其他错误后，上传目录中不应残留任何未完成的固件文件，磁盘空间应被正确释放。
- **ID 连续**：上传失败不应消耗 ID 计数器。多次成功上传固件时，ID 应严格连续递增（如 2→3→4→5），中间不因失败请求而出现跳号。
- 测试判定：GREEN（绿灯，缺陷已修复），测试命令退出码为 0。
- `go build ./...` 编译通过，`go vet ./...` 无静态警告。

## 6. 触发频率（Frequency）
100% 必现。只要触发 MD5 校验失败或版本冲突，两个问题都会稳定复现，无概率性。

## 7. 影响范围（Impact / Scope）
- 磁盘空间泄漏：MD5 校验失败时固件文件残留，频繁上传失败会导致磁盘空间被逐步耗尽。
- 数据一致性破坏：ID 不连续可能影响基于 ID 的排序、分页、统计逻辑，以及依赖连续 ID 的外部集成系统。
- 运维困扰：磁盘残留文件增加了运维清理负担，需要手动定期清理上传目录。

## 8. 附加说明（Additional Notes / Workaround）
目前可通过手动删除 uploads 目录下的残留文件来临时规避磁盘泄漏问题。ID 跳号问题暂无临时规避方案，需等待系统修复。建议在问题修复前，增加上传目录的定期清理任务作为临时缓解措施。