# 缺陷复现报告（Bug Reproduction Report）

## 1. 问题概述（Summary）

固件升级系统的批量上传功能在处理较大批量文件时出现资源耗尽异常。当一次性批量上传数百个固件包时，前面部分文件正常保存，后续文件逐渐失败，错误信息为 "too many open files"，表明进程文件描述符已被耗尽。单个文件上传和小批量上传均正常，仅在大批量场景下触发。

## 2. 环境信息（Environment）

- 操作系统：Linux（内核 4.15+）
- Go 版本：go1.21+
- 项目模块：fwupgrade
- 运行参数：测试时设置 ulimit RLIMIT_NOFILE=256
- 存储后端：FileStore（文件存储）
- CPU 核数：与并发无关，单进程即可复现

## 3. 复现步骤（Steps to Reproduce）

1. 进入项目根目录，执行 `go build ./...` 确保编译通过
2. 执行 `go test . -count=1 -run '^TestRedGreen$'` 运行红绿灯测试
3. 观察测试输出中的 RED/GREEN 判定结果
4. 如需手动复现，可编写程序调用批量上传接口，传入 300 个以上固件文件路径

## 4. 实际结果（Actual Behavior / Observed Output）

- 批量上传 300 个固件文件时，约 200 个成功，约 73 个失败
- 失败错误信息："failed to open firmware file: too many open files"
- 测试输出：RED（红灯，缺陷未修复）- success=227, fail=73
- 退出码：0（测试本身通过，仅打印 RED 诊断信息）

## 5. 期望结果（Expected Behavior）

- 批量上传 300 个固件文件全部成功，无文件句柄相关错误
- 测试输出：GREEN（绿灯，缺陷已修复）
- 所有固件文件正确保存并注册到系统中
- 进程文件描述符在整个批量操作过程中保持稳定
- go build ./... 编译通过
- go vet ./... 无警告

## 6. 触发频率（Frequency）

必现（100%）：当批量上传的文件数量超过进程文件描述符限制时稳定触发。默认 ulimit 1024 下约上传 800+ 文件触发；测试设置 ulimit 256 下约 200 文件即触发。

## 7. 影响范围（Impact / Scope）

- 批量上传接口在大批量场景下不可用，限制了固件批量导入能力
- 已成功上传的固件数据处于不一致状态（部分成功、部分失败），需要人工清理或重试
- 文件描述符泄漏可能影响同一进程内其他需要文件操作的功能
- 在高并发或大规模运维场景下，可能导致服务整体文件 I/O 阻塞

## 8. 附加说明（Additional Notes / Workaround）

临时规避方法：
- 拆分批量上传为小批次（每批不超过 50 个文件），每批之间适当延时
- 提高进程文件描述符限制（ulimit -n 65536），但无法根治问题
- 单文件上传模式不受影响，可作为应急替代方案
